export function WalletButton() {
  return (
    <button
      type="button"
      disabled
      className="w-full cursor-not-allowed rounded-xl border border-gray-600 bg-gray-800 px-6 py-4 text-lg font-semibold text-gray-400"
    >
      Wallet connections disabled — demo only
    </button>
  );
}
